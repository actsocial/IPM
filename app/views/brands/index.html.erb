<h1>Listing brands</h1>

<table  class="table">
  <tr>
    <th>Name</th>
    <th>Keyword</th>
    <th>Enable</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @brands.each do |brand| %>
  <tr>
    <td><%= brand.name %></td>
    <td><%= brand.keyword %></td>
    <td><%= brand.enable %></td>
    <td><%= link_to 'Show', brand %>
    <%= link_to 'Edit', edit_brand_path(brand) %>
    <%= link_to 'Destroy', brand, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Brand', new_brand_path %>
